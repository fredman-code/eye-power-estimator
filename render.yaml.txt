services:
  - type: web
    name: eye-power-estimator
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python app.py
    runtime: python
    pythonVersion: 3.10
    plan: free
    autoDeploy: true